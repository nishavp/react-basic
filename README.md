# react-basic - list of commands stepwise

npm init (To create package.json file)  
npm install -D parcel (if new project)  
npm install (if repo clone from github as all details are mentioned in package-lock.json)  
